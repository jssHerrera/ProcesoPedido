CREATE TABLE PRODUCTO(
    ID_PRODUCTO NUMERIC(10) NOT NULL PRIMARY KEY,
    NOMBRE VARCHAR2(120) NOT NULL UNIQUE,
    PRECIO NUMERIC(10,2) NOT NULL,
    STOCK NUMERIC(6) NOT NULL,
    ESTADO CHAR(1) DEFAULT '1' NOT NULL
);

CREATE SEQUENCE SEQ_PRODUCTO;

INSERT INTO PRODUCTO (ID_PRODUCTO, NOMBRE, PRECIO, STOCK)
VALUES(SEQ_PRODUCTO.NEXTVAL, 'LAPTOP  ASUS VivoBook Pro',3500,4);
INSERT INTO PRODUCTO (ID_PRODUCTO, NOMBRE, PRECIO, STOCK)
VALUES(SEQ_PRODUCTO.NEXTVAL, 'ALCOHOL ISOPROPILICO',8.75,10);

COMMIT;

UPDATE PRODUCTO SET NOMBRE='Servilletas ELITE', PRECIO=15.50, STOCK=12 WHERE ID_PRODUCTO=3;
COMMIT;

DELETE FROM PRODUCTO WHERE ID_PRODUCTO=3;
COMMIT;

SELECT ID_PRODUCTO, NOMBRE, PRECIO, STOCK,ESTADO FROM PRODUCTO;

/*
alter session set "_ORACLE_SCRIPT"=true;

*/


ALTER TABLE PRODUCTO
ADD (AUD_TIPO CHAR(1),
    AUD_IDUSUARIO NUMERIC(4),
    AUD_SESION VARCHAR2(60),
    AUD_IP VARCHAR2(20),
    AUD_FECHA DATE);

 SELECT ID_PRODUCTO, NOMBRE, PRECIO, STOCK,ESTADO,to_char(AUD_FECHA,'dd/mm/yyyy hh24:mi:ss') FECHA FROM PRODUCTO
 